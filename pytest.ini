[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore:.*timestamp seems very low.*:UserWarning
markers =
    slow: tests that render every font or layout combination
